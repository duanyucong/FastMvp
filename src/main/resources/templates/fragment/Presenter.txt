package ${packageName};

import com.dtcnet.skmvp.mvp.presenter.BasePresenter;

import javax.inject.Inject;

import androidx.lifecycle.Lifecycle;
import androidx.lifecycle.OnLifecycleEvent;

public class ${className}Presenter extends BasePresenter<${className}Contract.V, ${className}Contract.M> implements ${className}Contract.P {

    @Inject
    public ${className}Presenter(${className}Contract.V mView, ${className}Contract.M mModel) {
        super(mView, mModel);
    }
}